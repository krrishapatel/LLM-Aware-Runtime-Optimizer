# Runtime dependencies. Everything here is a real package on PyPI.
#
# The previous file listed torch-quantization and sagemaker-python-sdk, neither
# of which exists, so pip install -r requirements.txt failed on the first line
# it reached. It also pulled in boto3, sagemaker, jupyter, sphinx, matplotlib and
# seaborn, none of which any module imported.

torch>=2.0.0
transformers>=4.30.0
