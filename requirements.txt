# Core ML/AI dependencies
torch>=1.12.0
torchvision>=0.13.0
transformers>=4.20.0
datasets>=2.0.0
accelerate>=0.15.0

# MLIR and LLVM (macOS compatible)
# Note: MLIR bindings not available for macOS ARM64
# Using PyTorch-based alternatives instead
# mlir-python-bindings>=15.0.0
# llvm-python-bindings>=15.0.0

# TensorRT and CUDA (macOS compatible)
# Note: TensorRT not available on macOS, using PyTorch optimizations
# tensorrt>=8.0.0
# pycuda>=2022.1
# cupy-cuda11x>=11.0.0

# Quantization
torch-quantization>=0.1.0
pytorch-quantization>=2.1.0

# AWS and deployment
boto3>=1.26.0
sagemaker>=2.100.0
sagemaker-python-sdk>=2.100.0

# Performance and benchmarking
psutil>=5.9.0
memory-profiler>=0.60.0
line-profiler>=3.5.0

# Utilities
pyyaml>=6.0
click>=8.1.0
tqdm>=4.64.0
colorama>=0.4.5

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.8.0

# Development
black>=22.0.0
flake8>=5.0.0
mypy>=0.991

# Documentation
sphinx>=5.0.0
sphinx-rtd-theme>=1.0.0

# Jupyter and examples
jupyter>=1.0.0
ipykernel>=6.0.0
matplotlib>=3.5.0
seaborn>=0.11.0
